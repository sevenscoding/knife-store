export const ROUTE_NAMES = {
  CATALOG: 'Catalog',
  LOGIN: 'Login'
} as const

export const ROUTE_PATHS = {
  ROOT: '/',
  LOGIN: '/login'
} as const

export const ROUTE_META = {
  REQUIRES_AUTH: 'requiresAuth'
} as const